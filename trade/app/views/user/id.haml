= haml :'partials/profile', :locals => { :account => Models::System.instance.fetch_account(params[:id].to_i),
                                         :user => true,
                                         :active_items => active_items }

