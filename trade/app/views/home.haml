%h2 Content
%br
#usr_img
  %img(height=200 width=200){ :alt => Models::System.instance.fetch_user(session[:user]).name, :src => Models::System.instance.fetch_user(session[:user]).avatar }

%br
= "Currently logged in User: #{Models::System.instance.fetch_user(session[:user]).name}"
%br
= "Your Credits: #{Models::System.instance.fetch_user(session[:user]).credits}"
%br
= "You have #{Models::System.instance.fetch_user(session[:user]).list_items.size} active Items"
%br
= "You have #{Models::System.instance.fetch_user(session[:user]).list_items_inactive.size} inactive Items"

