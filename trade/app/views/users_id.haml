%h2= "Users (#{params[:id]}) Active Items"
%br
%ul
  - for item in active_items
    %li
      %em= item.name
      %em= item.price
      \:
      %a(href="/buy/#{item.id}.#{item.name}" ) Buy this item
