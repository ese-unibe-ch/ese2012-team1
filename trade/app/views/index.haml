#content
  %h1 Welcome to the Trading System!
  %br
  %h3 Preview:
  %div(id='preview')
    - for item in items_to_show
      %div(class='item_preview')
        %img(class='preview_img' height=200 width=200){ :alt => "#{item.name}", :src => "#{item.picture}" }
        %div= item.name
        %div(class='bigmargin')
          Description:
          %br
          = item.description
        %div(class='bigmargin')
          Price:
          %br
          = item.price
