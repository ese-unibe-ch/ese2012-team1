%div(id = "message-info")
  %a(href="/messagebox" name="messagebox")
    Messages
    - new_messages = Messenger.instance.get_message_box(session[:user]).new_messages_count
    - if new_messages > 0
      = "( #{new_messages} )"
    %img(alt = "Message" id="header-logo" src = "/images/message.png")