%h2 My Active Items
%br
%ul
  - for item in active_items
    %li
      %em= item.name
      %em= item.price
      \:
      %a(href="/changestate/#{item.id}.#{item.name}/setinactive" ) Set Inactive
