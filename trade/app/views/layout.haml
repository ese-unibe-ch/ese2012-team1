!!!
%html(xmlns="http://www.w3.org/1999/xhtml")
  %head
    %meta(http-equiv="Content-type" content="text/html;charset=UTF-8")
    %title Trading System - JOKR by #1
    - if defined? css
      %link(rel="stylesheet" href="/css/#{css}" type="text/css")
    - else
      %link(rel="stylesheet" href="/css/new_style.css" type="text/css")
    - if defined? script
      %script(type="text/javascript" src="/java-script/#{script}")

  %body{ defined?(onload) ? {:onload => "#{onload}"} : "" }
    = render_file('frame_header.haml')
    = render_file('frame_menu_logged.haml') if session['auth']
    = render_file('frame_menu_unlogged.haml') if !session['auth']
    %div(id="main")
      = yield
    = render_file('frame_footer.haml')