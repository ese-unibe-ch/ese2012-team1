%h2 All Users
%br
%ul
  - for user in all_users
    %li
      %em= user.name
      %em= " has #{user.list_items.size} Items to sell"
      \:
      %a(href="/users/#{user.id}" ) Show Users Items
